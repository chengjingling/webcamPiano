// Note class to create individual notes
class Note {
  constructor(noteSize, notePos, noteState, notePitch) {
    this.noteSize = noteSize;
    this.notePos = notePos;
    this.noteState = noteState;
    this.notePitch = notePitch;
  }
}